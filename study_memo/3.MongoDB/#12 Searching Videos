search를 해보자.

regular expression이라는게 있다. (정규 표현식)
string으로 부터 캐릭터를 가져오는 거 (regex)

옵션을 줄 수도 있다.

handsome이면 그거뿐만 아니라 비슷한것도 찾게 해주고 싶음(단어를 포함하는 것들)
mongoose의 regex 사용하면 됌/ 대소문자 구분 한하기 위해 옵션준다.
i = insensitive

이제 backend 50%
이제 프론트에 집중

댓글은 front에서 ajax와 바닐라js로 만들것
: 비디오 추가할때 마다 form형태로 어떤것을 back-end에 보내는데
새로고침이 있다. js로 하고싶은건 댓글달때 다 작성하고 엔터누르면 api로 댓글보내서
새로고침 없이 같은 위치로 오는것 . 마치 실시간 처럼.

댓글창 만드는데 배열 1개일때랑 2개이상일때 if문으로 나눈이유는?
단순함 그냥 1개인데 comments 되면 안되니까 ㅋㅋㅋ